name: mustcompany_quest
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_bloc: ^8.1.6
  image: ^4.2.0
  get_it: ^7.2.0
  path_provider: ^2.1.3
  build_runner: ^2.4.11

dev_dependencies:
  flutter_test:
    sdk: flutter

  bloc_test: ^9.1.7
  mockito: ^5.4.4
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
